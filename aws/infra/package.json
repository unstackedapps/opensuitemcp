{
  "name": "opensuitemcp-infra",
  "version": "1.0.0",
  "private": true,
  "description": "AWS CDK app for OpenSuiteMCP",
  "scripts": {
    "deploy": "cdk deploy --all",
    "diff": "cdk diff --all",
    "synth": "cdk synth",
    "destroy": "cdk destroy --all",
    "bootstrap": "cdk bootstrap",
    "test": "node -r ts-node/register --test --test-concurrency=1 test/security.test.ts"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.210.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.8.6",
    "aws-cdk": "^2.1029.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
